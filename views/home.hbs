<div class="main">
  <section class="hero-section section mt-5">
    <div class="container text-center">
       <h1 class="display-4">Te damos la bienvenida</h1>
       <br>
      <h3 class="display-4" id="username">{{currentUser.username}}</h3>
      <br>
      <p class="lead"style="font-size: xx-large;">Welcome to Ironhack-news 🌎</p>
    </div>
  </section>

  <div class="container text-center mt-4">
    <h1 class="display-4 font-weight-bold text-dark">Breaking <span class="text-danger">News</span></h1>
  </div>

  <br>

  <div class="container text-center banner d-flex justify-content-center align-items-center">
    <div class="video-like-container">
      <div class="video-like-screen">
        <a href="https://sergioironhacker.github.io/Digital-Space-Developer/" target="_blank">
          <img src="/images/banner.jpeg" alt="Banner" class="video-like-banner">
        </a>
      </div>
    </div>
  </div>

  <br>

  <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
    {{#each news as |singleNews|}}
      <div class="col">
        <div class="card" style="width: 100%; height: 100%;">

          <img src="{{singleNews.image}}" class="card-img-top" style="max-height: 200px; object-fit: cover;"
            alt="{{singleNews.title}}">
          <div class="card-body d-flex flex-column justify-content-between">
            <h5 class="card-title">{{singleNews.title}}</h5>
            <p class="card-text">{{singleNews.subtitle}}</p>
            <div class="mt-auto text-center">
              <a class="btn btn-primary" href="/news/{{singleNews._id}}">Leer más</a>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  </div>


  {{!-- Función para generar un color del usuario aleatorio en formato hexadecimal --}}

  <script>

    function getRandomColor() {
      const letters = '0123456789ABCDEF';
      let color = '#';
      for (let i = 0; i < 6; i++) {
        color += letters[Math.floor(Math.random() * 16)];
      }
      return color;
    }

    function changeUsernameColor() {
      const usernameElement = document.getElementById('username');
      setInterval(function () {
        const randomColor = getRandomColor();
        usernameElement.style.color = randomColor;
      }, 1000);
    }

    changeUsernameColor();
  </script>