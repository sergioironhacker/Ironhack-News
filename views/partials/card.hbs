
<h1>titulo de la noticia</h1>

<div class="card">
  <img src="{{news.image}}" class="card-img-top" alt="{{news.title}}">
  <div class="card-body">
    <h5 class="card-title">{{news.title}}</h5>
    <p class="card-text">{{news.subtitle}}</p>
    <a class="btn btn-primary" href="/news/{{news._id}}">Leer más</a>
  </div>
</div>